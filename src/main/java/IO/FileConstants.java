package IO;

import java.io.File;

public class FileConstants {

    private static final String sep = File.separator;

    public static final String POINTS_FILE_PATH = "src" + sep + "files" + sep + "points.txt";
    public static final String PROPERTIES_FILE_PATH = "src" + sep  + "main" + sep + "resources" + sep + "config.properties";
}
